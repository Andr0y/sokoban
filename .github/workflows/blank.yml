name: Mirroring

on: [push, pull_request]

env:
  MIRROR_URL: 'git@github.com:EpitechPromo2026/B-PSU-200-LYN-2-1-mysokoban-andrej.royan.git'

jobs:
  Mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          target_repo_url:
            ${{ env.MIRROR_URL }}
          ssh_private_key:
            ${{ secrets.SSH_PRIVATE_KEY }}
